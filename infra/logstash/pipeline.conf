input {
  kafka {
    bootstrap_servers => "kafka:9092"
    topics => ["telemetry.faults.v1"]
    group_id => "fleetlake-elasticsearch"
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "fleet-faults-%{tenantId}"
  }
}
